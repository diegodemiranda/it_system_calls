[project]
name = "it-system-calls"
version = "0.1.0"
description = "Sistema de gerenciamento de chamados de TI com persistência em CSV"
requires-python = ">=3.9"
dependencies = []
[project.optional-dependencies]
dev = [
	"pytest>=7.0",
]
